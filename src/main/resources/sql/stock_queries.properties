sql_add_ingredient = INSERT INTO stock \
  (person_id, ingredient_id, amount, measure_type) \
  VALUES (?, ?, ?, ?, ?);

sql_stock_read_by_person_id = SELECT person_id, ingredient_id, amount, measure_type FROM stock WHERE person_id = ?

sql_stock_update = UPDATE stock \
  SET amount=? \
  WHERE person_id = ? AND ingredient_id = ?;

sql_ingredient_delete = DELETE FROM stock WHERE person_id = ? ingredient_id = ?









